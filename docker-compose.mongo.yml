version: "3.9"

services:
  mongodb:
    image: mongo:7
    container_name: gobi-handaaaa-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: govihanda
    volumes:
      - ./ago-db/gobi-handaaaa/mongo-data:/data/db
